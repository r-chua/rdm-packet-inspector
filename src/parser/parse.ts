import type { ParseResult } from './types';

// TODO: Switch param to string and return to ParseResult
export const parseRdmPacket = (packet: string): ParseResult => {
  throw new Error('Not implemented yet');
};
